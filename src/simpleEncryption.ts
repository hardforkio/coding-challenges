// Taken from https://www.codewars.com/kata/simple-encryption-number-1-alternating-split/javascript

const encrypt = (text: string | null, n: number): string | null => {
    return ""
}

const decrypt = (encryptedText: string | null, n: number): string | null => {
    return ""
}

export { decrypt, encrypt }
